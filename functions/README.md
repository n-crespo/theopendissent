# Deploy Cloud Functions

> when you run `firebase deploy --only functions`, the CLI will look inside the
> functions/ folder and deploy only that code to Google's servers.
> It won't touch your src/ folder.

# Deploy Cloud Functions

```sh
firebase deploy --only database
```
