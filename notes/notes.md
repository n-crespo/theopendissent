
Database Key rules:

> If you create your own keys, they must be UTF-8 encoded, can be a maximum of
> 768 bytes, and cannot contain ., $, #, \[, \], /, or ASCII control characters 0-31
> or 127. You cannot use ASCII control characters in the values themselves,
> either.


i should utilize denormalization (split data into separate paths)
planned structure:

```

```
